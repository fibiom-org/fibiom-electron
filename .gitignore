node_modules
dist
out
.vite
.DS_Store
.eslintcache
*.log*
# but keep run logs committed under artifacts/ (scrub local paths/hostnames first)
!artifacts/*.log

# Local SQLite database (keep the db/ folder, ignore the data files)
db/*.sqlite
db/*.sqlite-*
db/auth.json

# Forge / electron-rebuild scratch
.forge-meta

# Generated by @qvac/sdk's bundleSdk (custom worker bundle) during start/package
qvac/
    